# Linux-dev
Commands to build and run version1:

  cd myFemtoShell_v1
  
  gcc *.c -o myFemtoShell

  ./myFemtoShell

Commands to build and run version2:

  cd myFemtoShell_v2 
  
  gcc *.c -o myFemtoShell

  ./myFemtoShell